@model IEnumerable<salaoDaLora.Models.Profissional>

@{
    ViewData["Title"] = "Lista de Profissionais";
}

<h1>@ViewData["Title"]</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Inserir Novo Profissional</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>Nome</th>
            <th>Especialidade</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Nome</td>
                <td>@item.Especialidade</td>
                <td>
                    <a asp-action="Details" asp-route-id="@item.ProfissionalId" class="btn btn-info">Detalhes</a>
                    <a asp-action="Edit" asp-route-id="@item.ProfissionalId" class="btn btn-warning">Editar</a>
                    <a asp-action="Delete" asp-route-id="@item.ProfissionalId" class="btn btn-danger">Excluir</a>
                </td>
            </tr>
        }
    </tbody>
</table>
